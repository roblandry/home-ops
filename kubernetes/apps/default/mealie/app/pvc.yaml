apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mealie-import
  namespace: default
spec:
  accessModes:
    - ReadOnlyMany
  storageClassName: copy-data
  resources:
    requests:
      storage: 1Gi
