apiVersion: ceph.rook.io/v1
kind: CephObjectStore
metadata:
  name: thanos-store
  namespace: rook-ceph
spec:
  metadataPool:
    replicated:
      size: 1
  dataPool:
    replicated:
      size: 1
  preservePoolsOnDelete: true
  gateway:
    port: 80
    instances: 1
