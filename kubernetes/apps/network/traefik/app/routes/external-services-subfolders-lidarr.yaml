---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.32.3/service.json
kind: Service
apiVersion: v1
metadata:
  name: lidarr
  namespace: network
spec:
  type: ExternalName
  ports:
    - name: http
      port: 8686
      targetPort: 8686
  externalName: ${SETTINGS_NAS_IP}
---
# yaml-language-server: $schema=https://json.schemastore.org/traefik-v3.json
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: lidarr-ingress
  namespace: network
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "apps.${SECRET_DOMAIN}"
    kubernetes.io/ingress.class: traefik
    gethomepage.dev/app: "lidarr"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "lidarr"
    gethomepage.dev/description: "Music Manager"
    gethomepage.dev/href: "https://apps.${SECRET_DOMAIN}/lidarr"
    gethomepage.dev/group: "Media Organization"
    gethomepage.dev/icon: "lidarr.png"
    # gethomepage.dev/widget.type: "lidarr"
    # gethomepage.dev/widget.url: "http://apps.${SECRET_DOMAIN}/lidarr"
    # gethomepage.dev/widget.key: "${HOMEPAGE_VAR_LIDARR_KEY}"
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`apps.${SECRET_DOMAIN}`) && PathPrefix(`/lidarr`)
      kind: Rule
      services:
        - name: lidarr
          port: 8686
          sticky:
            cookie:
              httpOnly: true
              secure: true
              sameSite: none
      middlewares:
        - name: internal-auth-middlewares
  tls:
    secretName: "${SECRET_DOMAIN/./-}-production-tls"

