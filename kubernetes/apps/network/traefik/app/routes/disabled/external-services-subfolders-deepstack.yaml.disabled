# DOES NOT WORK
http:

  #--------------------------------** ROUTERS **--------------------------------##
  routers:
    deepstack:
      entryPoints:
        - websecure
      middlewares:
        - internal-only@file
        - default-headers@file
        # - error-pages-middleware@docker
        # - middlewares-authentik@file
      rule: Host(`apps.${SECRET_DOMAIN}`) && PathPrefix(`/deepstack`)
      service: deepstack
      tls:
        certResolver: letsencrypt
        domains:
          - main: ${SECRET_DOMAIN}
            sans:
              - '*.${SECRET_DOMAIN}'

  #--------------------------------** SERVICES **-------------------------------##
  services:
    deepstack:
      loadBalancer:
        servers:
          - url: http://10.0.10.2:83

  #------------------------------** MIDDLEWARES **------------------------------##
  # middlewares:
