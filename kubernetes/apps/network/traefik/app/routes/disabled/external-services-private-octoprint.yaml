# SETTINGS_OCTO_IP
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.32.3/service.json
kind: Service
apiVersion: v1
metadata:
  name: octoprint
spec:
  type: ExternalName
  ports:
    - name: http
      port: 80
      targetPort: 80
  externalName: ${SETTINGS_OCTO_IP}
---
# yaml-language-server: $schema=https://json.schemastore.org/traefik-v3.json
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: octoprint-ingress
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`octo.${SECRET_DOMAIN}`)
      kind: Rule
      priority: 1
      services:
        - name: octoprint
          port: 80
      middlewares:
        - name: internal-noauth-middlewares
  tls:
    secretName: "${SECRET_DOMAIN/./-}-production-tls"
