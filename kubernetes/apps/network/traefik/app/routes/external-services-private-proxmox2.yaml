# SETTINGS_PVE2_IP
---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.32.3/service.json
kind: Service
apiVersion: v1
metadata:
  name: proxmox2
  namespace: network
spec:
  type: ExternalName
  ports:
    - name: https
      port: 8006
      targetPort: 8006
  externalName: ${SETTINGS_PVE2_IP}
---
# yaml-language-server: $schema=https://json.schemastore.org/traefik-v3.json
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: proxmox2-ingress
  namespace: network
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "pve2.${SECRET_DOMAIN}"
    kubernetes.io/ingress.class: traefik
    gethomepage.dev/app: "proxmox2"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "proxmox2"
    gethomepage.dev/description: "Proxmox Cluster"
    gethomepage.dev/href: "https://pve2.${SECRET_DOMAIN}"
    gethomepage.dev/group: "Infrastructure"
    gethomepage.dev/icon: "proxmox.png"
    # gethomepage.dev/widget.type: "proxmox"
    # gethomepage.dev/widget.url: "http://pve2.${SECRET_DOMAIN}"
    # gethomepage.dev/widget.username: "${HOMEPAGE_VAR_PVE2_USER}"
    # gethomepage.dev/widget.password: "${HOMEPAGE_VAR_PVE2_PASS}"
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`pve2.${SECRET_DOMAIN}`)
      kind: Rule
      priority: 1
      services:
        - name: proxmox2
          port: 8006
      middlewares:
        - name: internal-noauth-middlewares
  tls:
    secretName: "${SECRET_DOMAIN/./-}-production-tls"
