---
# yaml-language-server: $schema=https://raw.githubusercontent.com/yannh/kubernetes-json-schema/refs/heads/master/v1.32.3/service.json
kind: Service
apiVersion: v1
metadata:
  name: radarr
  namespace: network
spec:
  type: ExternalName
  ports:
    - name: http
      port: 7878
      targetPort: 7878
  externalName: ${SETTINGS_NAS_IP}
---
# yaml-language-server: $schema=https://json.schemastore.org/traefik-v3.json
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: radarr-ingress
  namespace: network
  annotations:
    external-dns.alpha.kubernetes.io/hostname: "apps.${SECRET_DOMAIN}"
    kubernetes.io/ingress.class: traefik
    gethomepage.dev/app: "radarr"
    gethomepage.dev/enabled: "true"
    gethomepage.dev/name: "radarr"
    gethomepage.dev/description: "Movie Manager"
    gethomepage.dev/href: "https://apps.${SECRET_DOMAIN}/radarr"
    gethomepage.dev/group: "Media Organization"
    gethomepage.dev/icon: "radarr.png"
    # gethomepage.dev/widget.type: "radarr"
    # gethomepage.dev/widget.url: "http://apps.${SECRET_DOMAIN}/radarr"
    # gethomepage.dev/widget.key: "${HOMEPAGE_VAR_RADARR_KEY}"
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`apps.${SECRET_DOMAIN}`) && PathPrefix(`/radarr`)
      kind: Rule
      services:
        - name: radarr
          port: 7878
          sticky:
            cookie:
              httpOnly: true
              secure: true
              sameSite: none
      middlewares:
        - name: internal-auth-middlewares
  tls:
    secretName: "${SECRET_DOMAIN/./-}-production-tls"
