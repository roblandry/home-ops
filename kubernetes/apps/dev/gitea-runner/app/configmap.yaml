apiVersion: v1
kind: ConfigMap
metadata:
  name: dind-daemon
  namespace: dev
data:
  daemon.json: |-
    {
      "dns": ["10.43.0.10"],
      "dns-search": ["svc.cluster.local","cluster.local"]
    }
